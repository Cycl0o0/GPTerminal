name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
          - os: ubuntu-latest
            goos: linux
            goarch: arm64
          - os: macos-latest
            goos: darwin
            goarch: amd64
          - os: macos-latest
            goos: darwin
            goarch: arm64
          - os: windows-latest
            goos: windows
            goarch: amd64
          - os: windows-latest
            goos: windows
            goarch: arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Run tests
        run: go test ./...

      - name: Build
        shell: bash
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          bin="gpterminal-${{ matrix.goos }}-${{ matrix.goarch }}"
          ext=""; if [ "${{ matrix.goos }}" = "windows" ]; then ext=".exe"; fi
          # GITHUB_REF_NAME is the pushed tag (e.g. v3.2.1); strip the leading v
          # and inject it so the binary reports its real release version.
          version="${GITHUB_REF_NAME#v}"
          go build -ldflags="-s -w -X github.com/cycl0o0/GPTerminal/cmd.Version=${version}" -o "${bin}${ext}" .
          tar czf "${bin}.tar.gz" "${bin}${ext}" LICENSE

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: gpterminal-${{ matrix.goos }}-${{ matrix.goarch }}
          path: "*.tar.gz"

  publish:
    needs: release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: "*.tar.gz"
